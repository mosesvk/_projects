TODO Update Summary:

- Higher Ed
  - Match FusionChart background colors (green, red, yellow) to Capin color scheme.

- K12
  - Match header and navigation styling to church projects.
  - Implement comp-style API loading.
  - Add a Print button to the sidebar and a print modal ("Generate reports" / "Print presentation").
  - Move all print/Excel functionality from the options modal into the new print modal.
  - Use Toastify for filter/enrollment changes and Run success:
      - On Run, show a success toast with unique clients count.
      - On filter change, show a toast with the previous unique clients count and prompt user to click Run to refresh.

- Church (both)
  - Update "CFHI" title to use the K-12 header font and color.
