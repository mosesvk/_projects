 

Higher Ed (08_higherEducation)
- [08_higherEducation] FusionChart colors to match capin colors. For this to-do: only the Fusion Chart background colors need to match the capin colors. Within 07_international, in the source and charts folder, make sure only the Fusion Chart background colors match the original config capin colors. The green, the red, and the yellow need to match the capin colors.

K12 (04_k12)
- [04_k12] Make sure the header/nav matches with church. This is complex on the UI side. Reference the images above: K-12 header vs church headers. Match the organization of Index.html, how buttons are styled and arranged next to years. Reference the HTML and header files of 05_cfhi_comp and 06_cfhi_standard for how 04_k12 should look.
- [04_k12] Make sure the print functionality matches church projects (reference 05_cfhi_comp, 06_cfhi_standard). This includes:
- making sure the print button is on the sidebar
- making sure when we click that print button, it displays a print_modal where we can click "generate reports" or "print presentation". Move all print/Excel functionality from options_modal into the print_modal. Create print_modal, print button on sidebar (like church). Reference church's print_base64, base URL file, and print Excel file. Implement in 04_k12.

Church (05_cfhi_comp & 06_cfhi_standard)
- [05_cfhi_comp & 06_cfhi_standard] Update both church projects. In each Index.html header, make the title styling (font and color) match the K-12 header title. Keep the text "CFHI" but apply the same font and color as the K-12 title. Reference images above for K-12 title styling.