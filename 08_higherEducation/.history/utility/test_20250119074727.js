import { getSumOfArray, getAverageOfArray } from './Utility';

const array = [2, 2, 3, 3, 4, 4]

const sum = getSumOfArray(array)

const avg = getAverageOfArray(array)

console.log('test.js', {sum, avg})